var element = document.getElementById("slider-home-0");

// Check if the element exists
if (element) {
    // Remove the 'mouseover' event listener from the element
    element.removeEventListener("mouseover", yourFunction, false);
}
